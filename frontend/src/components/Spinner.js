import React from "react";
import "../styles/Spinner.css"; // We'll add the CSS next

const Spinner = () => (
  <div className="spinner-container">
    <div className="loader"></div>
  </div>
);

export default Spinner;
